import { FleetView } from "../checklist/components/FleetView";

export default function ComprasVeiculosPage() {
    return (
        <div className="animate-fade-in">
            <FleetView embedded={false} />
        </div>
    );
}
